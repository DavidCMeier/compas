/* Versión de la app — se incrementa en cada despliegue.
   IMPORTANTE: al subirla, actualiza también los ?v=X.Y.Z de index.html
   (cache busting: fuerzan la descarga de los ficheros nuevos). */
const APP_VERSION = { number: '1.5.2', date: '2026-08-12' };
